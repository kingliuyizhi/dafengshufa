<%= form_for(@announcement, :url => {:controller => "admin/admin_announcements", :action => "destroy"}, :method => :delete) do |f| %>
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h3 id="myModalLabel" class="text-warning">删除文章</h3>
    </div>
    <div class="modal-body">
        您确定要删除序号为<%= @announcement.id %>的文章吗？
    </div>
    <div class="modal-footer">
        <button class="btn" data-dismiss="modal" aria-hidden="true">取消</button>
        <%= f.submit "删除", :class => "btn btn-success" %>
    </div>
<% end %>